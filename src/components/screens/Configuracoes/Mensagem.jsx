import React, { Component } from "react";

class Mensagem extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div>
        a
      </div>
    );
  }
}

export default Mensagem;
