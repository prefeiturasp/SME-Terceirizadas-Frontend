import React from "react";

import { reduxForm } from "redux-form";
 
const FormUnifiedSolicitation = (props) => {
    
    return (
        <div>
            <form onSubmit={this.props.handleSubmit}>
                
            </form>
        </div>
    )
}